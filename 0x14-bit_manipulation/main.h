#ifndef MAIN_H
#define MAIN_H

#include <stddef.h>
int _putchar(char);
unsigned int binary_to_uint(const char *);
unsigned int _to_uint(char);
int _strlen(const char *);
unsigned int _pow(unsigned int , unsigned int);

#endif
