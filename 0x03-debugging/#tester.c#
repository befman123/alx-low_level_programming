#include <stdio.h>
#include <stdbool.h>
int main(void)
{
	int a = 972;
	int b = -98;
	int c = 0;
    int largest;

    if (a > b && b > c)
    {
	    
    printf("%d", largest);
    return (largest);
}
